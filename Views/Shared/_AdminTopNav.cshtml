@model WebApplication1.Models.BaseViewModel
<!-- Admin Top Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid px-4">
        <a class="navbar-brand" href="@Url.Action("AdminDash", "Admin")">
            <img src="~/images/logo1.png" alt="Logo" width="40"> Nustar Admin
        </a>
        <div class="d-flex align-items-center ms-auto">
            <span class="text-white me-3 fw-bold">
                <i class="fas fa-user-shield me-1"></i> Admin
            </span>
            <a href="@Url.Action("Logout", "Home")" class="btn btn-outline-light">
                <i class="fas fa-sign-out-alt me-1"></i> Logout
            </a>
        </div>
    </div>
</nav> 